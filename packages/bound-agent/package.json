{
  "name": "@antseed/bound-agent",
  "version": "0.1.10",
  "description": "Bound agent runtime for AntSeed — monetize knowledge with read-only, knowledge-augmented AI agents",
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "node ../../scripts/remove-paths.mjs dist",
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@antseed/node": ">=0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@antseed/node": "workspace:*"
  }
}
